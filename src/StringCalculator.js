function Add(array) {
    var sum = 0;
    

    for (let i in array) {
        if (isNaN(array[i])) {
            let x = String.array[i];
            x = split.array[i];

            for(i < x.length; i++;) {
                if (x[i]){
                    
                }
            }
        }
        else {
            sum = sum + parseInt(array[i]);
        }  
    }
    return sum;
}